services:
  hkoweather-bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - bot-state:/app/state
    environment:
      STATE_FILE: /app/state/.hkoweather_bot_state.json

volumes:
  bot-state:
