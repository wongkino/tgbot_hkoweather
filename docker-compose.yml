services:
  hkoweather-bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - bot-state:/app/state
    environment:
      STATE_FILE: /app/state/hkoweather-bot-state.json

volumes:
  bot-state:
